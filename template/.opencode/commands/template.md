---
# The file name becomes the command name. Rename this file when you turn it into
# a real command.
description: Describe what this command helps the user do.
# Optional valid fields you can enable later:
# agent: build
# model: anthropic/claude-sonnet-4-5
# subtask: true
---
Explain the task this command should perform.

- Use `$ARGUMENTS` if the command accepts free-form arguments.
- Use `$1`, `$2`, ... if the command expects positional arguments.
- Reference files with `@path/to/file`.
