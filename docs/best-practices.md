# Repository best practices

- Keep template-repo documentation in `docs/`.
- Keep generated-project files under `template/`.
- Prefer removing optional template pieces over carrying unused scaffolding.
- Validate the generated project, not only the template source.
